version: "3"
networks:
  web:
    external: true
services:
  epping_bottle:
    build: bottle
    ports:
      - 8088:8080
    networks:
      - web
    labels:
      - "traefik.enable=true"
      - "traefik.port=80"
      - "traefik.frontend.rule=Host:epping.dev.urbanobservatory.ac.uk"